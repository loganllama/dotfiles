# dotfiles
dotfiles for ona environments


## Manual Steps (for Claude)

* CLAUDE.local.md is intended for the obsidian root directory, but I'm not sure if this is run before or after the first clone happens.
* settings.local.json is intended for obsidian/.claude/, same thing, copy it over

update: well, let's try `safe_obsidian_symlink` first and see if it works.
